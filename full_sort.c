#include "push_swap.h"

int calculateScore()
{
	//calculateBScore;
	//calculateAScore;
}
int full_sort(t_list stack_a, t_list stack_b)
{
	findMinMaxMed(stack_a);
	fillStackB();
	while stack_b is not null
	{
		calculateScore();
		makeDecisioin();
		moveToStackA();
	}
}